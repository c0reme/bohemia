export const host = 'localhost';
export const port = 5432;
export const database = 'bohemia';
export const user = 'admin';
export const password = 'admin';

export default { host, port, database, user, password };